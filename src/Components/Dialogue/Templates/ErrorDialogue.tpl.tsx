import React from 'react';

const
  ErrorDialogueTpl = (props) =>
    <div className={`tp-dialogue-modal-content-inner`}>
      <div className={`tp-dialogue-modal-content-inner-icon`}></div>

      {props.children}</div>;

export default ErrorDialogueTpl;

